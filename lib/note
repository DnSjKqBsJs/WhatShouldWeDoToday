// Autocomplete<CountryResult>(
            //   optionsBuilder: (tripDestination) async {
            //     if (tripDestination.text == '') {
            //       return const Iterable<CountryResult>.empty();
            //     }
            //     return await GeocodingService().getCountry(
            //       tripDestination.text,
            //     );
            //   },
            //   displayStringForOption: (CountryResult option) => option.name,

            //   onSelected: (CountryResult selectedCountry) {
            //     print(selectedCountry.name);
            //   },
            // ),



            // controller: search,
                        // decoration: InputDecoration(
                        //   border: OutlineInputBorder(),
                        //   filled: true,
                        // ),
                        // onChanged: (value) async {
                        //   _searchResults = await FoursquareService()
                        //       .searchPlaces(
                        //         search.text,
                        //         Provider.of<AppState>(
                        //           context,
                        //           listen: false,
                        //         ).currentTrip!.centerLat,
                        //         Provider.of<AppState>(
                        //           context,
                        //           listen: false,
                        //         ).currentTrip!.centerLng,
                        //       );
                        // },